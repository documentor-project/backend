spring:
  application:
    name: documentor-backend

  datasource:
    url: jdbc:h2:mem:documentor
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
